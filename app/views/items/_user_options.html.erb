<div class="user-settings">
  <p class="title">Admin Panel</p>
  <% if @item.bids.count == 0 %>
    <p><%= link_to 'Edit item', edit_item_path(@item) %></p>
  <% end %>
  <p class="delete-account"><%= link_to 'Delete item', @item, method: :delete, data: { confirm: 'Are you sure?' } %></p>
</div>